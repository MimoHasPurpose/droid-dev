import 'package:flutter/material.dart';

void main() {
  runApp(  MaterialApp(
    home: Scaffold(
      appBar:AppBar(
        title: const Text('mimo is titlle'),
        backgroundColor: Colors.brown[700],
        centerTitle: true,
      ),
      body: const Text('hello mimossssa!'),
    )
  ));
}
